# API Server

The public Android release download is served at:

`/api/download/ludo-star-bd.apk`

The endpoint returns the signed APK with an attachment content disposition so
Android browsers download it instead of opening it as plain text.